import { Text } from 'react-native';

export default function H2({ children }) {
  return (
    <Text className="font-comfortaa text-3xl text-center my-4 text-green-900">
      {children}
    </Text>
  );
}
